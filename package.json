{
  "name": "react-simple-qr-code-scanner",
  "version": "1.5.2",
  "description": "A super simple qr code scanner",
  "scripts": {
    "build": "nx build react-simple-qr-code-scanner",
    "test": "nx dev test",
    "publish": "npm publish ./packages/components/",
    "publish:beta": "pnpm i && nx build components && npm publish ./packages/components/ --tag beta",
    "publish:alpha": "pnpm i && nx build components && npm publish ./packages/components/ --tag alpha",
    "prepublish": "pnpm i && nx build components"
  },
  "dependencies": {},
  "devDependencies": {
    "nx": "16.5.0",
    "nx-cloud": "latest"
  }
}
